<div class="container">
  <h2>受注履歴一覧</h2>
  <div class="row">
    <div class="col">
      <table class="table table-striped">
        <thead>
        </thead>
        <tbody>
          <% @orders.each do |f| %>
          <div class="border-top">
            <span>購入日</span>
          </div>
            <% f.histories.each do |a| %>
              <div class="item-history">
                <%= attachment_image_tag a, :item_profile, :fill, 100, 100, format: 'jpeg' %></br>
                  <div class="item-information">
                    <p class="p7"><%= a.item_name %></p>
                    <p class="p7"><%= a.artist_name %></p>
                    <span>枚数</span>
                    <span><%= a.quantity %></span>
                    <p class="p4"><%= a.subtotal.round.to_s(:delimited, delimiter: ',') %>円(税込)</p>
                  </div>
              </div>
            <% end %>
            <div class="Address-information">
              <p>送付先</p>
              <p>
                <span>名前:</span>
                <span><%= f.address.name %></span>
              </p>
              <p>
                <span>電話番号:</span>
                <span><%= f.address.phone_number %></span>
              </p>
              <p>
                <span>郵便番号:</span>
                <span><%= f.address.postcode %></span>
              </p>
              <span>住所</span>
              <p><%= @tests[f.address.prefecture_id] %><%= f.address.address %></p>
              <P><%= f.status %></P>
             <span><%= link_to '配送状況を変更する',edit_admins_order_path(f.id) %></span>
              <p>価格総計</p>
              <p class="p4"><%= f.total_price.round.to_s(:delimited, delimiter: ',')%>円(税込)</p>
            </div>
          <% end %>
        </tbody>
      </table>
      <%= paginate @orders, class: "paginate" %>
    </div>
  </div>
</div>

<style>
  .Address-information{
    width: 200px;
    position: relative; bottom: 250px; left:800px;
  }
  
  .item-history{
    width: 300px;
    position: relative; top: 40px; left: 70px;
  }
  .item-information{
     position: relative; bottom: 100px; left: 200px;
  }
  .border-top{
    width: 100%;
    border: 0.5px solid #bbb;
  }
  .border-down{
    width: 100%;
    border: 0.3px solid #bbb;
  }

}
</style>