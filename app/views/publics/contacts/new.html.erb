<div class="container">
	<div class="row">
		<div class="col text-center">
			<h1>お問い合わせフォーム</h1>
		</div>
	</div>
	<div class="row">
		<div class="col text-center">
			<%= form_for (@contact) do |f| %>
				<%= f.text_field :contact_content %>
				<%= f.submit "送信", class: "btn btn-primary" %>
			<% end %>
		</div>
	</div>
</div>

<style>

input[type="text"] {
        width: 800px;
        height: 200px;
    }
</style>

